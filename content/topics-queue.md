# Blog Topics Queue

Topics are published in order (top to bottom). Check off a topic once it has been published.
Add new topics anywhere in the Queue section -- the scheduler always picks the first unchecked item.

## Queue

- [x] Drizzle ORM migrations -- generate vs push, when to use each, and how to safely run them in production
- [ ] Stripe webhooks in Next.js -- handling idempotency, event replay, and signature verification
- [ ] Role-based access with JWT -- adding org-level roles without an extra DB query on every request
- [ ] Prompt caching with Claude -- cut Anthropic API costs up to 90% on long system prompts
- [ ] SWR mutations -- optimistic UI updates without Redux or useEffect
- [ ] Neon DB branching -- preview deployments with isolated database branches on every PR
- [ ] Resend + react-email -- sending transactional email from Next.js App Router with type-safe templates
- [ ] Cloudinary image uploads -- a reusable upload hook and API route pattern for Next.js
- [ ] Protected API routes in Next.js -- JWT auth, HttpError, and the thin-route convention
- [ ] Zod validation in full-stack TypeScript -- one schema for forms, API, and DB without duplication
- [ ] Multi-tenancy with organizations -- slug routing, role hierarchy, and per-org context in React
- [ ] Drizzle relations -- why db.query with `with` silently fails and how to fix it every time
- [ ] OG image generation in Next.js -- static root images and dynamic per-post images with ImageResponse
- [ ] Sitemap and robots.txt in Next.js App Router -- dynamic segments, DB-fetched slugs, and disallow rules
- [ ] JSON-LD for blog posts -- injecting BlogPosting and BreadcrumbList schemas for rich search results
- [ ] Next.js loading and error boundaries -- when to use loading.tsx, error.tsx, and Suspense together
- [ ] Claude tool use in Next.js -- building an agentic loop that calls your own API as a tool
- [ ] Streaming AI responses with Server-Sent Events -- wiring Claude to a useAiChat hook end to end
- [ ] bcryptjs password hashing -- cost factor tradeoffs and safe patterns for register and login routes
- [ ] Dark mode with next-themes -- CSS variable theming without flash of unstyled content
- [ ] Recharts in Next.js -- server-fetched data, responsive containers, and dark mode compatible colors
- [ ] Drizzle array columns -- storing tags as text[] in Postgres and querying with arrayContains
- [ ] Comparison landing pages -- /vs/[competitor] routes, static generation, and SEO metadata patterns
- [ ] Feature flags in Next.js -- env-var driven flags without a third-party service
- [ ] Claude Code skills -- writing custom slash commands that encode your project's conventions

## Done

<!-- Completed topics are moved here automatically by the scheduler -->
